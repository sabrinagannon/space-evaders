#!/usr/bin/env python
# -*- coding: utf-8 -*-

# Each array of cutscene text contains an array for each
# line of text in that cutscene. Each line array is
# organized as follows: [text, time_to_wait].
text = {
    "intro_cutscene" : [["INTRO TEXT LINE ONE", 1500],
                        ["INTRO TEXT LINE TWO", 1000],
                        ["INTRO TEXT LINE THREE", 500]],
    "level1_cutscene" : ["LEVEL ONE TEXT"],
    "level2_cutscene" : ["LEVEL TWO TEXT"]
}
